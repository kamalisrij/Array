class Solution {
    public int heightChecker(int[] heights) {
        int arr[]=heights.clone();
        Arrays.sort(arr);
        int c=0;
        for(int k=0;k<heights.length;k++)
        {
            if(arr[k]!=heights[k])
                c++;
        }
        return c;
        
    }
}
